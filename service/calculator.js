let plusFunction = (first, second) => first + second;
let subtractFunction = (first, second) => first - second;
let addOne = amount => amount + 1;

module.exports = {
  plus: plusFunction,
  subtract: subtractFunction,
  addOne
}